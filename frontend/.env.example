# For local dev with Vite proxy: leave empty (proxy handles /api → localhost:4000)
# For production (Vercel): set to your Render backend URL
VITE_API_BASE=https://business-verification-api.onrender.com
